name: Twingate Headless Connector

on:
  workflow_dispatch:

jobs:
  twingate-connector:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Install Twingate Headless Client
        run: |
          echo "🔧 Twingate Client kuruluyor..."
          echo "deb [trusted=yes] https://packages.twingate.com/apt/ /" | sudo tee /etc/apt/sources.list.d/twingate.list
          sudo apt update -yq
          sudo apt install -yq twingate

      - name: Setup and Start Twingate
        env:
          TWINGATE_SERVICE_KEY: ${{ secrets.TWINGATE_SERVICE_KEY }}
        run: |
          echo "$TWINGATE_SERVICE_KEY" | sudo twingate setup --headless=-
          sudo twingate start
          echo "✅ Twingate bağlantısı kuruldu"
          sleep 10

      - name: (Optional) Twingate Status
        run: |
          sudo twingate status

      - name: Show Connector IP
        run: |
          IP=$(hostname -I | awk '{print $1}')
          echo "Connector IP: $IP"
          echo "TWINGATE_IP=$IP" >> $GITHUB_ENV

      - name: Keep Connector Alive
        run: |
          echo ""
          echo "=== TWINGATE CONNECTOR ACTIVE ==="
          echo "IP: $TWINGATE_IP"
          while true; do
            echo "[ $(date) ] Connector çalışıyor..."
            sleep 300
          done
